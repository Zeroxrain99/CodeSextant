---
tier: 全文
type: 使用日誌（階段1 實際拿來用的真實記錄 + 心得）
updated: 2026-06-18
---

# CodeSextant 使用日誌（2026-06-18，階段1「實際拿來用」）

> user 指令：C6-a→階段2 硬化→**階段1 實際拿來用、實際記錄使用日誌和心得**→階段4。
> 本檔＝在真實 repo 上實際用 codesextant 的逐次記錄＋心得，數據皆真實工具輸出（非腦補）。
> 環境：daemon 跑在隔離 port **8791**（不干擾全機共用的 8790），用最新代碼（含階段2 硬化），跑完已 stop 清殘留。

## 一、逐次使用記錄

### 使用1：CodeSextant 自己 — 全局地圖（map）
指令：`codesextant_query.py --project <CodeSextant> --action map --budget 1200`
結果：daemon 在 8791 detached 起來、map 出 100 個符號（PageRank 排序）。
⚠ **真實踩坑**：第一次 map 顯示的符號是**陳舊的**——出現 `_iter_python_files @ engine.py:34`（這是 C5a 改名前的舊名，當前代碼是 `_iter_source_files`）、行號也對不上剛改的代碼。
根因：wrapper 的 auto-index **只在「完全未索引」時才建**；庫已存在（之前索引過舊代碼）就用舊庫、不主動更新。

### 使用2a：發現 stale → 手動 reindex --force
指令：`--action reindex --force`
結果：`新建/更新 14 檔、跳過 0、移除 0、錯誤 0；共 206 個符號，耗時 0.255 秒`。→ 索引更新到最新代碼。

### 使用2b：refs — `find_references` 被誰呼叫
指令：`--action refs --target find_references --src-root <CodeSextant>`
結果：**同名定義 3 處**（client.py:89 / engine.py:239 / references.py:146），wrapper 用第一個（client.py:89）。
→ 高信心 **0 處**、低信心 36 處、純名稱比對框到 9 檔/37 次命中、**誤判率 100%**。
觀察：這是坑2「同名符號要 `--def-path`」的活案例——查一個 3 處同名的符號、沒指定定義，jedi 用了 client 那個方法定義，但呼叫點呼叫的是別的同名 → goto 都指向別處 → 高信心 0。**wrapper 有列出 3 個候選定義（好），但高信心 0 對不熟的人可能困惑**。

### 使用2c：refs — 我新加的 `_refs_non_python`（自我驗證）
指令：`--action refs --target _refs_non_python --src-root <CodeSextant>`
結果：定義 engine.py:221、**高信心 2 處**（engine.py:287、engine.py:290）、低信心 1 處、誤判率 50%。
觀察：287/290 正是我在 `find_references` 裡呼叫 `_refs_non_python` 的兩處（fallback 分支 + else 分支）。**codesextant 用自己的引擎，精確找到我剛加代碼的真實呼叫點＝自我驗證硬化已在最新代碼且 jedi 真解析準確。**

### 使用3a：TS repo（ai-king-vscode/src）— 全局地圖
指令：`--project <ai-king-vscode/src> --action map --budget 400`
結果：33 個 TS 符號，**type / interface / function / class / method / variable 全分類正確**（modelsForAgentKind function、AgentKind type、AgentKindDef interface、AuthError class…）。C5a 多語言抽符號在真 TS repo work。

### 使用3b：TS refs — `modelsForAgentKind` 被誰呼叫（驗 C5b ts-morph）
指令：`--action refs --target modelsForAgentKind --src-root <ai-king-vscode/src>`
結果：定義 agent-kinds.ts:92、**高信心 2 處**（bridge-view.ts:97、bridge-view.ts:3416）、低信心 0、誤判率 0%。
觀察：C5b ts-morph 在真 TS repo **跨檔**精確找到引用。⚠ 但 wrapper 文案寫死「jedi 真 import 解析」，TS 實際走 ts-morph，文案沒區分（小瑕疵）。

### 端到端 daemon HTTP 驗證（坑6/7 走真 HTTP，非單元測試）
工具：`tools/_phase1_http_verify.py`（urllib 真 HTTP）。結果全綠：
| 檢查 | 結果 |
|---|---|
| GET /health | 200、service=codesextant、就緒=True |
| GET /status 預設 | 無 `git_stale`（坑7-1 lazy 不 spawn git）✅ |
| GET /status?fresh=1 | git_stale=False、indexed_sha=current=a42df042（坑6）✅ |
| POST 本機 Origin | 200（放行 loopback）✅ |
| POST 外部 Origin evil.com | 403（擋跨站）✅ |
| POST https://tauri.localhost | 200（坑7-2 Tauri v2 放行）✅ |
| POST http://127.0.0.1.evil.com | 403（擋前綴繞過）✅ |

## 二、心得

**真好用的地方（核心賣點被真實驗證）**
1. **真 import 解析碾壓名稱比對是真的**：CodeSextant 自己 `find_references` 純名稱比對 37 次命中→真引用 0（誤判 100%）；TS `modelsForAgentKind` 名稱比對 2 次→真引用 2（誤判 0%、跨檔）。jedi/ts-morph 把「同名雜訊」濾掉的價值，在實際 repo 上立刻看得到。
2. **多語言同一介面**：Python（jedi）+ TS（ts-morph）走同一組指令、同樣輸出格式，TS 符號分類與跨檔引用都準。
3. **單例冪等**：多次 ensure 同 PID（28208）不重開，全代理共用設計 work。
4. **自我驗證閉環**：codesextant 找到我剛加的 `_refs_non_python` 的真實呼叫點——工具能對自己的代碼導航，是很強的可信度信號。
5. **階段2 硬化端到端全綠**：坑6/7 在真 daemon HTTP 層全部驗證通過。

**踩到的真實 UX 問題（誠實）**
1. ⚠ **索引陳舊最痛**：auto-index 只在「完全未索引」才建，已索引就吃舊庫——我第一次 map 看到的是改名前的舊符號。要記得手動 `reindex --force`。**坑6 已經把 git HEAD sha 記進庫了，但目前 freshness 是 lazy（要 `?fresh=1`）且 wrapper 的 map/refs 沒自動比對 sha 提示「該 reindex」**。這是最該補的一哩。
2. **同名符號高信心 0 的困惑**：查常見名（find_references 有 3 同名）不指定 `--def-path` → 高信心 0，對不熟的人像「壞了」。wrapper 已列候選定義，但缺一句「高信心 0 可能因同名、試 --def-path」的引導。
3. **文案綁死「jedi」**：TS 走 ts-morph，wrapper 卻一律印「jedi 真 import 解析」。
4. **`daemon stop` 對非預設 port 沒清乾淨**：本次用隔離 port 8791，`python -m codesextant.daemon stop` 後 8791 仍 LISTEN（要手動 `Stop-Process -Id` 才清）。預設 8790 應正常；非預設 port 的 stop 疑似沒讀 `CODESEXTANT_PORT`（邊角，但跨代理共用/多 port 場景值得查）。

**改進建議（沉澱，留待未來——非本階段 scope）**
- **A（高價值）**：wrapper 在 map/refs 前自動做 git sha freshness 比對（坑6 的 `indexed_git_sha` 已在庫、`_git_head_sha` 已有），stale 就印一行「⚠ 索引落後 git HEAD，建議 reindex」。把坑6 的能力接到日常 UX，直接解 UX 問題1。
- **B**：wrapper 找引用文案「jedi 真 import 解析」→「真 import 解析（Python=jedi／TS=ts-morph）」。
- **C**：refs 高信心 0 且候選定義 ≥2 時，加一行「偵測到 N 個同名定義、高信心 0，試 `--def-path` 指定真實定義」。

## 三、結論
CodeSextant 核心價值（真 import 解析、多語言、單例共用）在真實 repo 上**實測成立**；階段2 硬化端到端全綠。最大的日常 UX 缺口＝**索引陳舊沒主動提示**（坑6 已備好 sha、差 wrapper 接線＝改進 A）。整體「拿來用」順手、輸出可信、是改碼前掌握全局的有效導航。
